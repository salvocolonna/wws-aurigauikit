import { defineMessages } from "react-intl"

const PATH = "image-viewer"

export default defineMessages({
  upload: {
    id: `${PATH}.upload`,
    defaultMessage: "Upload..."
  },
  edit: {
    id: `${PATH}.edit`,
    defaultMessage: "Edit"
  }
})
